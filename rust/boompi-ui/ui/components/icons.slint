// SVG iconography, tinted at runtime via `Image.colorize` (the SVG's own
// fill is just an alpha mask).
//
// Sources:
//  - ../icons/*.svg from the Geist icon set (geist-icons npm package, MIT -
//    see ../icons/LICENSE-geist-icons.txt), matching Vercel's geistcn icons.
//  - spotify.svg + bluetooth.svg from SVG Repo (CC0 collections).
//  - gamepad.svg from Font Awesome Free (CC BY 4.0, fontawesome.com).
//
// Slint renders these with resvg at display resolution, so they stay crisp
// at any size. Requires i-slint-core's `svg` feature, which both the winit
// (desktop) and linuxkms (Pi) backends enable in our feature graph.

export component IconPlay inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/play-fill.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconPause inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/pause.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconPrev inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/skip-back-fill.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconNext inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/skip-forward-fill.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconSpeaker inherits Image {
    in property <color> icon-color: white;
    /// 0 = muted, 1 = quiet, 2 = loud.
    in property <int> level: 2;
    source: root.level == 0 ? @image-url("../icons/speaker-off-fill.svg")
        : root.level == 1 ? @image-url("../icons/speaker-volume-quiet-fill.svg")
        : @image-url("../icons/speaker-volume-loud-fill.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconSpotify inherits Image {
    in property <color> icon-color: #1DB954;
    source: @image-url("../icons/spotify.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

// Geist "shareplay" glyph: display with a triangle rising through it.
export component IconAirplay inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/shareplay.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconBluetooth inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/bluetooth.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconSettings inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/settings-gear-fill.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconGamepad inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/gamepad.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconBolt inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/lightning.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconBack inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/chevron-left.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

// Album-art placeholder.
export component IconNote inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/musical-notes.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

// AirPlay icon-preset tiles (project-drawn; the HomePod highlights are
// luminance masks so colorize's alpha-only tinting keeps them).
export component IconAirplayGeneric inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/airplay-generic.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconAirplayHomePodMini inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/airplay-homepod-mini.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconAirplayHomePod inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/airplay-homepod.svg");
    colorize: root.icon-color;
    image-fit: contain;
}

export component IconAirplayAppleTV inherits Image {
    in property <color> icon-color: white;
    source: @image-url("../icons/airplay-appletv.svg");
    colorize: root.icon-color;
    image-fit: contain;
}
