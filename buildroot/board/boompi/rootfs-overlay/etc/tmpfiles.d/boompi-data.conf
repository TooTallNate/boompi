# Subdirectories on the persistent /data partition (created after the
# mount, before the services that bind/point into them).
d /data/bluetooth       0700 root root -
d /data/nm-connections  0700 root root -
d /data/cache           0755 root root -
d /data/bin 0755 root root -
