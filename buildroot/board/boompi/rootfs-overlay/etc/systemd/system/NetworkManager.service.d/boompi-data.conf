# NM reads its keyfile store from /data (see NetworkManager.conf); make
# sure the partition is mounted and the directory created first.
[Unit]
RequiresMountsFor=/data
After=systemd-tmpfiles-setup.service
