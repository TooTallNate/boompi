# Per-box binaries (/data/bin) visible to services and systemd-run,
# ahead of the image paths. /data/bin is root-only and not exported
# anywhere (the SMB share is scoped to /data/games).
[Manager]
DefaultEnvironment=PATH=/data/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
