# Onboarding captive portal: while the boompi-ap shared connection is up,
# NM runs this dnsmasq for DHCP/DNS. Answer every DNS query with our own
# address (NM shared-mode gateway) so phones' captive-portal probes land on
# boompid's HTTP server, which redirects to the setup page.
address=/#/10.42.0.1
