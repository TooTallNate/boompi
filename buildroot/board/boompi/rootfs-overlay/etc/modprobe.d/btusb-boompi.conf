# btusb URB completion serialization (poll_sync) is compiled in via
# CONFIG_BT_HCIBTUSB_POLL_SYNC=y (see board/boompi/linux-bt.fragment
# for the full story) - the old `options btusb force_poll_sync=1` line
# that used to live here targeted a parameter this kernel renamed, and
# only produced "unknown parameter ignored" at boot. Verify at
# runtime: /sys/kernel/debug/bluetooth/hci0/force_poll_sync == Y.
