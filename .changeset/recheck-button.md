---
"boompi": patch
---

Allow re-checking for updates while an update offer is already shown
(panel and web): a newer build can supersede the stored offer between
the six-hourly automatic checks, especially on the edge channel.
