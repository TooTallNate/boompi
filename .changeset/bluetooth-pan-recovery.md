---
"boompi": minor
---

Add an isolated Bluetooth PAN recovery network for SSH from a previously
paired Linux computer when Wi-Fi or the main application is unavailable.
The recovery service uses existing SSH keys and does not provide Internet
sharing or expose the web UI and file shares over Bluetooth.
