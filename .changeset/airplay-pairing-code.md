---
"boompi": minor
---

Optional AirPlay pairing code (off by default): when enabled, phones
must enter a code - shown on the speaker's screen and in the web UI -
once per device before they can stream; reconnects are silent. Also
brings back the bookshelf icon, which rides the same pairing
advertisement. Enable it under "AirPlay device icon" in the web
settings.
