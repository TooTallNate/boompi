# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Build artifact: boompid embeds it (rust-embed). Local builds run
# `make web` first (deploy-appliance.sh does this automatically); CI
# builds it in the shared `web` job and the image jobs download it.
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
