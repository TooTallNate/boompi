# George's box - panel service environment (systemd EnvironmentFile).
#
# Display rotation needs no entry here anymore: boompi-ui (and the
# game launcher) derive it from the kernel's DRM panel-orientation
# hint, which the dtparam=rotate=270 in config.txt publishes - one
# declaration of how the panel is mounted. Setting
# SLINT_KMS_ROTATION here still works and overrides the hint.
